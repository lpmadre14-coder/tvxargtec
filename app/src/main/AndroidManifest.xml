<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />

    <!-- Características para TV -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:name=".TvxargtecApp"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tvxargtec"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Splash / Launch -->
        <activity
            android:exported="true"
            android:name=".activity.SplashAty"
            android:screenOrientation="fullSensor"
            android:theme="@style/Theme.Tvxargtec.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Categoría para Android TV -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:exported="false" android:name=".activity.OnboardingAty" android:screenOrientation="fullSensor" android:theme="@style/Theme.Tvxargtec.Onboarding" />
        <activity android:exported="false" android:name=".activity.PinAty" android:screenOrientation="fullSensor" />
        
        <!-- Main Activity: Quitamos portrait para que gire en TV -->
        <activity 
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|layoutDirection" 
            android:exported="true" 
            android:launchMode="singleTask" 
            android:name=".activity.MainAty" 
            android:screenOrientation="fullSensor" />

                <activity android:exported="false" android:name=".mine.activity.LoginAty" android:screenOrientation="fullSensor" />

        <!-- Play Activity: Landscape preferido para TV -->
        <activity 
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|layoutDirection" 
            android:launchMode="singleTop" 
            android:name=".activity.PlayAty" 
            android:screenOrientation="sensorLandscape" 
            android:supportsPictureInPicture="true" />

        <activity android:name=".activity.MyFavListActivity" android:screenOrientation="fullSensor" />
        <activity android:name=".activity.RecordsAty" android:screenOrientation="fullSensor" />
        <activity android:name=".activity.ColumnListAty" android:screenOrientation="fullSensor" />
        <activity android:name=".activity.SearchAty" android:screenOrientation="fullSensor" />
        <activity android:name=".activity.WebViewAty" android:screenOrientation="fullSensor" />
        <activity android:name=".activity.SettingLanguageAty" android:screenOrientation="fullSensor" />
        <activity android:name=".mine.activity.NotificationSettingAty" android:screenOrientation="fullSensor" />
        <activity android:name=".mine.activity.AboutAty" android:screenOrientation="fullSensor" />

        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.tvxargtec.online.cast.CastOptionsProvider" />

        <service android:exported="false" android:name=".service.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service android:exported="false" android:foregroundServiceType="mediaPlayback" android:name=".service.AudioPlaybackService" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_path" />
        </provider>

    </application>

</manifest>
