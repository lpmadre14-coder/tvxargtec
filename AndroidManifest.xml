<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" 
    package="com.tvxargtec.online">

    <!-- Permisos para Streaming -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <!-- Compatibilidad con Android TV -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.screen.landscape" android:required="false" />

    <application 
        android:name=".TvxargtecApp"
        android:icon="@mipmap/ic_logo" 
        android:label="@string/app_name" 
        android:largeHeap="true" 
        android:supportsRtl="true" 
        android:theme="@style/MobileAppTheme" 
        android:usesCleartextTraffic="true">

        <!-- Splash Screen con soporte TV -->
        <activity android:exported="true" android:name=".activity.SplashAty" android:screenOrientation="fullSensor" android:theme="@style/AppTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Actividad Principal: Soporta giro completo -->
        <activity 
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize" 
            android:exported="true" 
            android:launchMode="singleTask" 
            android:name=".activity.MainAty" 
            android:screenOrientation="fullSensor">
        </activity>

        <!-- Reproductor: Forzado a Horizontal para mejor experiencia en TV -->
        <activity 
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize" 
            android:launchMode="singleTop" 
            android:name=".activity.PlayAty" 
            android:screenOrientation="sensorLandscape" 
            android:supportsPictureInPicture="true"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize"/>

        <activity android:name=".mine.activity.AccountAty" android:screenOrientation="fullSensor"/>
        <activity android:name=".mine.activity.AboutAty" android:screenOrientation="fullSensor"/>
        
        <!-- Google Cast Options -->
        <meta-data android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME" android:value="com.tvxargtec.online.cast.CastOptionsProvider"/>

        <service android:exported="false" android:name=".service.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <provider android:authorities="${applicationId}.fileprovider" android:exported="false" android:grantUriPermissions="true" android:name="androidx.core.content.FileProvider">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_path"/>
        </provider>

    </application>
</manifest>
